name: v3_cap_web
description: A professional cinematic camera app for the web.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.10.5+9
  image: ^4.1.3
  url_launcher: ^6.2.6 # For opening the ad link
  universal_html: ^2.2.4 # For triggering the download
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
